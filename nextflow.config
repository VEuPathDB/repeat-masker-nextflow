params.inputFilePath = "$baseDir/data/Data.fa"
params.outputDir = "$baseDir/data"
params.trimDangling = false
params.rmParamsFile = "$baseDir/rmParams"
params.dangleMax = 0
params.seqCleanerPath = "$baseDir/seqCleaner.pl"

process {
    withName:repeatMasker {
        container = 'dfam/tetools:latest'
    }
}
docker {
    enabled = true
}
