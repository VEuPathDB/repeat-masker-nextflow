params {
  inputFilePath = "$baseDir/data/test.fa"
  fastaSubsetSize = 60
  trimDangling = false
  dangleMax = 0
  rmParams = "-xsmall"
  outputFileName = "blocked.seq"
  errorFileName = "blocked.err"
  outputDir = "$launchDir/output"
}
process {
  container = 'veupathdb/repeatmasker:latest'
}
docker {
    enabled = true
}
